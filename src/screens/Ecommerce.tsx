import { LoginScreenProps } from 'props'
import * as React from 'react'
import Wrapper from '../components/Wrapper'

const LoginScreen: React.FC<LoginScreenProps> = () => {
  return <Wrapper>Ecommerce</Wrapper>
}

export default LoginScreen
